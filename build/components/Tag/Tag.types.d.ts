import { Intent } from '../../common/';
export interface TagProps {
    fill?: boolean;
    intent?: Intent;
    interactive?: boolean;
    large?: boolean;
    minimal?: boolean;
    round?: boolean;
}
