import {Intent} from '../../common';
import {IconName} from '../Icon/IconName';
export interface CalloutProps {
    // icon?: 


    intent?: Intent;

    heading?: string;
    iconName?: IconName;
}
